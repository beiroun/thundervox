version: "3"
services:
  kamailio:
    image: kamailio/kamailio:latest
    ports:
      - "5060:5060/udp"
      - "5060:5060/tcp"
    volumes:
      - ./kamailio.cfg:/usr/local/etc/kamailio/kamailio.cfg
